// SPDX-License-Identifier: Apache-2.0
pragma solidity 0.8.29;

abstract contract EnterableErrors {
    error NotSufficientEntryFee();
    // error EntryAlreadyExists(address player);
    // error EntryNotFound(address player);
    // error EntryNotActive(address player);
    // error EntryNotRefundable(address player);
}
